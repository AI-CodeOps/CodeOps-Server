package com.codeops.mcp.entity.enums;

/**
 * Deployment environment for MCP sessions and developer profiles.
 *
 * <p>Represents the target deployment environment that an AI agent
 * session operates against or that a developer defaults to.</p>
 */
public enum Environment {
    LOCAL,
    DEVELOPMENT,
    STAGING,
    PRODUCTION
}
